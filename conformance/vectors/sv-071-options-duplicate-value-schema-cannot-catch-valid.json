{
  "id": "sv-071-options-duplicate-value-schema-cannot-catch-valid",
  "class": "schema-validation",
  "description": "DELIBERATELY VALID, and the vector exists to say so. Two options share a `value` but differ in `label`, so they are distinct array items and `uniqueItems` cannot see the collision \u2014 JSON Schema has no uniqueness-by-sub-property keyword. The rule is real and this submit MUST be rejected `422` by a conformant Hub (dp-026); this vector pins the exact boundary of what the SCHEMA can prove, so nobody later reads a green vector run as evidence the rule is enforced here.",
  "ref": "spec \u00a75.2, \u00a712 (v0.6)",
  "target": "v0.6/message.schema.json",
  "expect": "valid",
  "input": {
    "ma2h_version": "0.6",
    "type": "ask",
    "created_at": "2026-09-13T10:00:00Z",
    "agent": {
      "id": "deploybot/dev-team",
      "run_id": "run_01H",
      "runtime": "cli"
    },
    "title": "Ship or hold?",
    "idempotency_key": "idem-01",
    "request": {
      "mode": "select",
      "options": [
        {
          "value": "approve",
          "label": "Approve and deploy"
        },
        {
          "value": "approve",
          "label": "Approve but hold"
        }
      ]
    }
  }
}
