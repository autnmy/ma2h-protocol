{
  "id": "sv-045-directive-legacy-hash-destination-valid",
  "class": "schema-validation",
  "description": "Back-compat pin: a 0.4-era directive addressed to a legacy hash-bearing principal (agent:foo#bar) still validates — the §4 session grammar applies only to >= 0.5 directives, so an upgrade never strands queued legacy mail (the session-qualified interpretation never applies to pre-0.5 payloads).",
  "ref": "spec §13.2; MIGRATION (unchanged-wire contract)",
  "target": "v0.5/inbound-message.schema.json",
  "expect": "valid",
  "input": {
    "ma2h_version": "0.4",
    "type": "directive",
    "id": "dir_01J5DIR0003",
    "from": "human:alice",
    "to": "agent:foo#bar",
    "created_at": "2026-08-10T12:00:00Z",
    "title": "Legacy hash-bearing destination queued before the upgrade"
  }
}
