{
  "id": "sv-078-input-schema-type-array-malformed-invalid",
  "class": "schema-validation",
  "description": "A `type` ARRAY must itself be well-formed, not merely contain \"object\". JSON Schema requires every entry to be one of the seven primitive type names, so `[\"object\", 42]` is invalid and ajv REFUSES TO COMPILE it \u2014 leaving the ask unanswerable exactly as a scalar schema does. Widening the rule to admit arrays (sv-075) must not widen it into admitting malformed ones.",
  "ref": "spec \u00a75.2 (v0.6)",
  "target": "v0.6/message.schema.json",
  "expect": "invalid",
  "input": {
    "ma2h_version": "0.6",
    "type": "ask",
    "created_at": "2026-09-13T10:00:00Z",
    "agent": {
      "id": "deploybot/dev-team",
      "run_id": "run_01H",
      "runtime": "cli"
    },
    "title": "t",
    "idempotency_key": "k",
    "request": {
      "mode": "input",
      "schema": {
        "type": [
          "object",
          42
        ],
        "properties": {
          "r": {
            "type": "string"
          }
        }
      }
    }
  }
}
