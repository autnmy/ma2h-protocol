{
  "id": "sv-051-capability-inter-agent-without-ack-invalid",
  "class": "schema-validation",
  "description": "A capability document advertising inter_agent.enabled without the ack primitive is rejected — the leg's delivery-honesty lifecycle rides the §14 track, so advertising inter_agent without ack is non-conformant (§8.0).",
  "ref": "spec §8.0, §14 (v0.5)",
  "target": "v0.5/capability.schema.json",
  "expect": "invalid",
  "input": {
    "ma2h_version": "0.5",
    "inter_agent": {
      "enabled": true,
      "entry_kinds": ["message", "response", "receipt"],
      "sender_allowlists": true
    }
  }
}
