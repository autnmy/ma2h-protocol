{
  "id": "sv-079-input-schema-type-array-duplicate-invalid",
  "class": "schema-validation",
  "description": "The same, for uniqueness: JSON Schema requires a `type` array to have unique items, so `[\"object\",\"object\"]` does not compile. Pinned separately from sv-078 because the two failures have different causes and an implementation could easily catch one and not the other.",
  "ref": "spec \u00a75.2 (v0.6)",
  "target": "v0.6/message.schema.json",
  "expect": "invalid",
  "input": {
    "ma2h_version": "0.6",
    "type": "ask",
    "created_at": "2026-09-13T10:00:00Z",
    "agent": {
      "id": "deploybot/dev-team",
      "run_id": "run_01H",
      "runtime": "cli"
    },
    "title": "t",
    "idempotency_key": "k",
    "request": {
      "mode": "input",
      "schema": {
        "type": [
          "object",
          "object"
        ],
        "properties": {
          "r": {
            "type": "string"
          }
        }
      }
    }
  }
}
